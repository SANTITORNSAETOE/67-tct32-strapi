version: '3'

services:
  app:
    image: 
    container_name: 
    ports: 
    
    environment:
      - DATABASE_CLIENT=
      - DATABASE_HOST=
      - DATABASE_PORT=
      - DATABASE_NAME=
      - DATABASE_USERNAME=
      - DATABASE_PASSWORD=
      - DATABASE_SSL=
  db:
    image: postgres:lastest
    container_name: santitorn-db
    ports:
      
    environment:
      - POSTGRES_PASSWORD=
      - POSTGRES_USER=
      - POSTGRES_DB=
